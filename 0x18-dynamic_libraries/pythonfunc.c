#include <stdio.h>

/* header */

/**
* add - adds twp numbers
* @a: int numbere to add
* @b: int number to add
*
* Return: a + b
*/
int add(int a, int b)
{
	return (a + b);
}


/**
 * add - adds twp numbers
 * @a: int number
 * @b: int number
 *
 * Return: a - b
 */
int sub(int a, int b)
{
	return (a - b);
}


/**
 * add - adds twp numbers
 * @a: int number
 * @b: int number
 *
 * Return: a * b
 */
int mul(int a, int b)
{
	return (a * b);
}


/**
 * add - adds twp numbers
 * @a: int number
 * @b: int number
 *
 * Return: a / b
 */
int div(int a, int b)
{
	return (a/b);
}


/**
 * add - adds twp numbers
 * @a: int number
 * @b: int number
 *
 * Return: a % b
 */
int mod(int a, int b)
{
	return (a % b);
}
