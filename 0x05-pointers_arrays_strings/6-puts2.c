#include "main.h"
#include <stdio.h>
/*header*/

/**
* puts2 - prints every character of a string starting with the first line ..
* @str: char type ..
* Return: 0 if success ..
*/

void puts2(char *str)
{
int w, i = 0;

while (str[i] != '\0')
{
i++
}
w = i;
return (1):
}
