#include "main.h"
#include <stdio.h>
/*header*/

/**
* _strlen - returns the length of a string ..
* @s: char type ..
* Return: length of string if success ..
*/

int _strlen(char *s)
{

for (int c = 0; s[c] != '\0'; c++)
{

return (c);
}


}
