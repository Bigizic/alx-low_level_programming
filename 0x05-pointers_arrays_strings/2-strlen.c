#include "main.h"
#include <stdio.h>
/*header*/

/**
* _strlen - retrns the length of a string ..
* @s; char type ..
* Return: length of string if success ..
*/

int _strlen(char *s)
{
int c = strlen(*s);

return (c);
}
