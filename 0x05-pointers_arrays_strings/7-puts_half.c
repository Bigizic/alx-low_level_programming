#include "main.h"
#include <stdio.h>
/*header*/

/**
* puts_half - prints half of a string ..
* @str: char type .
* Return: 0 if success ..
*/

void puts_half(char *str)
{
int w, i =0;

while (str[i] != '\0')
{
i++;
}
w = i;
return (1);
}
